language: ruby
rvm:
- "2.0"
- "2.1"
- "2.2"
- ruby-head
matrix:
  allow_failures:
  - rvm: ruby-head
  fast_finish: true
before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq libslp-dev
